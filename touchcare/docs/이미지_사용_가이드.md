# 이미지 사용 가이드

## 📁 이미지 배치 위치

### 권장 위치: `assets/images/` 폴더

```
touchcare-mobile/
└── assets/
    ├── fonts/
    │   └── PretendardVariable.ttf
    └── images/              ← 여기에 이미지 배치
        ├── logo.png         ← 로고 이미지
        ├── logo@2x.png      ← 고해상도 (선택사항)
        ├── logo@3x.png      ← 초고해상도 (선택사항)
        └── ...
```

### 폴더 생성 (없다면)

```bash
cd touchcare-mobile
mkdir -p assets/images
```

또는 직접 폴더 생성:
- `assets` 폴더 안에 `images` 폴더 만들기

---

## 🖼️ 이미지 사용 방법

### 1. Image 컴포넌트 사용

```typescript
import { Image } from 'react-native';

// 방법 1: require 사용 (권장)
<Image 
  source={require('../assets/images/logo.png')} 
  style={{ width: 100, height: 100 }}
/>

// 방법 2: 상대 경로
<Image 
  source={require('../../assets/images/logo.png')} 
  style={{ width: 100, height: 100 }}
/>
```

### 2. 로고 이미지 예시 (LoginScreen)

```typescript
import React from 'react';
import { View, Image, StyleSheet } from 'react-native';

export const LoginScreen = () => {
  return (
    <View style={styles.container}>
      <Image 
        source={require('../../assets/images/logo.png')}
        style={styles.logo}
        resizeMode="contain"
      />
      {/* 나머지 로그인 UI */}
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  logo: {
    width: 150,
    height: 150,
    marginBottom: 30,
  },
});
```

---

## 📐 이미지 크기 권장사항

### 로고 이미지
- **기본**: 200x200px ~ 300x300px
- **고해상도**: 
  - `logo.png` (1x) - 200x200px
  - `logo@2x.png` (2x) - 400x400px (선택사항)
  - `logo@3x.png` (3x) - 600x600px (선택사항)

### 이미지 포맷
- **PNG**: 투명 배경이 필요한 경우 (로고 등)
- **JPG**: 사진, 복잡한 이미지
- **SVG**: 벡터 이미지 (react-native-svg 필요)

---

## 🎨 Image 컴포넌트 주요 속성

```typescript
<Image
  source={require('../assets/images/logo.png')}
  style={styles.logo}
  resizeMode="contain"      // 이미지 크기 조절 방식
  // resizeMode 옵션:
  // - 'contain': 비율 유지하며 전체 표시
  // - 'cover': 영역을 채우며 비율 유지
  // - 'stretch': 영역에 맞춰 늘림
  // - 'center': 중앙 정렬
  // - 'repeat': 반복
/>
```

---

## 📂 실제 사용 예시

### LoginScreen에 로고 추가

```typescript
// src/screens/LoginScreen.tsx
import { Image } from 'react-native';

// ... 기존 코드 ...

<View style={styles.content}>
  {/* 로고 추가 */}
  <Image 
    source={require('../../assets/images/logo.png')}
    style={styles.logo}
    resizeMode="contain"
  />
  
  <Text style={styles.title}>터치케어</Text>
  <Text style={styles.subtitle}>로그인</Text>
  
  {/* 나머지 폼 */}
</View>

// 스타일 추가
const styles = StyleSheet.create({
  // ... 기존 스타일 ...
  logo: {
    width: 120,
    height: 120,
    marginBottom: 20,
  },
});
```

---

## 🔍 경로 확인

### 현재 파일 위치에 따른 경로

```
src/screens/LoginScreen.tsx
  ↓
assets/images/logo.png
  ↓
경로: ../../assets/images/logo.png
```

**계산 방법:**
- `src/screens/` → `../` (src로)
- `src/` → `../` (루트로)
- `assets/images/logo.png`

---

## ✅ 체크리스트

- [ ] `assets/images/` 폴더 생성
- [ ] 로고 이미지 파일 배치
- [ ] `Image` 컴포넌트 import
- [ ] `require()` 경로 확인
- [ ] `style`로 크기 지정
- [ ] `resizeMode` 설정 (선택사항)

---

## 💡 팁

1. **이미지 최적화**: 앱 크기를 줄이기 위해 이미지를 최적화하세요
2. **고해상도**: 다양한 화면 크기를 지원하려면 @2x, @3x 버전 준비
3. **캐싱**: Expo는 이미지를 자동으로 캐싱합니다
4. **에러 처리**: 이미지 로드 실패 시 `onError` 핸들러 사용

---

**이미지를 `assets/images/` 폴더에 넣고 위 방법대로 사용하시면 됩니다! 🎨**
