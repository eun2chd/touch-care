# TouchCare 앱 로드 순서 및 페이지 전환 흐름

## 🚀 앱 시작부터 화면 표시까지

### 1단계: 진입점 (Entry Point)

**파일: `index.js`**
```javascript
import { registerRootComponent } from 'expo';
import App from './App';  // ← App.tsx를 찾아서 import

registerRootComponent(App);  // ← React Native에 App 컴포넌트 등록
```

**역할:**
- Expo가 앱을 시작할 때 가장 먼저 실행되는 파일
- `App.tsx` (또는 `App.js`)를 찾아서 로드
- TypeScript를 사용하면 자동으로 `.tsx` 확장자를 찾음

**우선순위:**
1. `App.tsx` (TypeScript)
2. `App.js` (JavaScript)
3. 없으면 에러

---

### 2단계: 메인 앱 컴포넌트

**파일: `App.tsx`**

#### 2-1. 폰트 로딩
```typescript
const [fontsLoaded] = useFonts({
  'Pretendard-Regular': require('./assets/fonts/PretendardVariable.ttf'),
  // ...
});
```

**역할:**
- 프리텐다드 폰트 파일을 메모리에 로드
- 폰트가 로드될 때까지 스플래시 스크린 유지

#### 2-2. 화면 상태 초기화
```typescript
const [currentScreen, setCurrentScreen] = useState<Screen>('Login');
```

**역할:**
- 현재 보여줄 화면을 상태로 관리
- **초기값: `'Login'`** ← 첫 화면이 로그인 화면인 이유!

#### 2-3. 화면 렌더링
```typescript
const renderScreen = () => {
  switch (currentScreen) {
    case 'Login':
      return <LoginScreen navigation={navigation} />;
    case 'Home':
      return <HomeScreen navigation={navigation} />;
    // ...
  }
};
```

**역할:**
- `currentScreen` 상태에 따라 해당 화면 컴포넌트를 렌더링
- `'Login'`이면 `LoginScreen` 컴포넌트를 반환

---

### 3단계: 로그인 화면 표시

**파일: `src/screens/LoginScreen.tsx`**

**역할:**
- ID와 비밀번호 입력 필드 제공
- 로그인 버튼 클릭 시 검증
- 성공 시 `navigation.navigate('Home')` 호출

---

## 🔄 페이지 전환 흐름

### 로그인 → 홈 화면

```
1. 사용자가 ID/PW 입력 후 "로그인" 버튼 클릭
   ↓
2. LoginScreen의 handleLogin() 실행
   ↓
3. ID/PW 검증 (touch / ntp1231)
   ↓
4. 성공 시: navigation.navigate('Home') 호출
   ↓
5. App.tsx의 navigation 객체가 실행됨
   navigation = {
     navigate: (screen) => setCurrentScreen(screen)
   }
   ↓
6. setCurrentScreen('Home') 실행
   ↓
7. App.tsx가 리렌더링됨
   ↓
8. renderScreen()에서 'Home' 케이스 실행
   ↓
9. <HomeScreen navigation={navigation} /> 렌더링
   ↓
10. 홈 화면 표시! ✅
```

---

## 📁 파일 구조와 역할

```
touchcare-mobile/
│
├── index.js                    # 1️⃣ 진입점 - App.tsx를 찾아서 등록
│
├── App.tsx                     # 2️⃣ 메인 앱 - 폰트 로딩, 화면 상태 관리
│   ├── 폰트 로딩
│   ├── currentScreen 상태 ('Login' 초기값)
│   └── renderScreen() - 화면 선택
│
└── src/
    └── screens/
        ├── LoginScreen.tsx     # 3️⃣ 로그인 화면 (첫 화면)
        ├── HomeScreen.tsx      # 4️⃣ 홈 화면 (로그인 후)
        └── DetailScreen.tsx    # 5️⃣ 상세 화면
```

---

## 🎯 핵심 포인트

### 왜 로그인 화면이 첫 화면인가?

```typescript
// App.tsx
const [currentScreen, setCurrentScreen] = useState<Screen>('Login');
//                                              ↑
//                                    초기값이 'Login'
```

**이 한 줄이 첫 화면을 결정합니다!**

### 화면 전환은 어떻게?

```typescript
// App.tsx에서 navigation 객체 생성
const navigation = {
  navigate: (screen: Screen) => setCurrentScreen(screen),
  //                              ↑
  //                    상태를 변경하면 리렌더링됨
};

// LoginScreen에서 사용
navigation.navigate('Home');  // ← setCurrentScreen('Home') 실행
```

**React의 상태 업데이트 → 자동 리렌더링 → 새 화면 표시**

---

## 🔍 로드 순서 요약

```
1. index.js 실행
   ↓
2. App.tsx import 및 등록
   ↓
3. App.tsx의 useFonts() - 폰트 로딩 시작
   ↓
4. currentScreen = 'Login' 초기화
   ↓
5. 폰트 로딩 완료까지 스플래시 스크린
   ↓
6. 폰트 로딩 완료 → renderScreen() 실행
   ↓
7. currentScreen이 'Login'이므로 LoginScreen 렌더링
   ↓
8. 로그인 화면 표시! ✅
```

---

## 🎨 실제 코드 흐름 예시

### 앱 시작 시
```typescript
// App.tsx
export default function App() {
  // 1. 폰트 로딩
  const [fontsLoaded] = useFonts({...});
  
  // 2. 화면 상태 초기화 (첫 화면 = 'Login')
  const [currentScreen, setCurrentScreen] = useState<Screen>('Login');
  
  // 3. 폰트 로딩 완료되면
  if (!fontsLoaded) return null;
  
  // 4. 화면 렌더링
  return renderScreen();  // → LoginScreen 반환
}
```

### 로그인 성공 시
```typescript
// LoginScreen.tsx
const handleLogin = () => {
  if (id === 'touch' && password === 'ntp1231') {
    navigation.navigate('Home');  // ← App.tsx의 setCurrentScreen('Home') 실행
  }
};

// App.tsx
const navigation = {
  navigate: (screen) => setCurrentScreen(screen)  // ← 상태 변경
};

// 상태 변경 → 리렌더링 → renderScreen() → HomeScreen 반환
```

---

## 💡 이해하기 쉬운 비유

**앱 = 책**
- `index.js` = 책의 첫 페이지 (목차)
- `App.tsx` = 책의 목차 페이지 (어떤 챕터를 보여줄지 결정)
- `currentScreen` = 현재 읽을 챕터 번호
- `LoginScreen` = 1장 (첫 번째 챕터)
- `HomeScreen` = 2장 (두 번째 챕터)

**페이지 넘기기:**
- `navigation.navigate('Home')` = "2장으로 넘기기"
- `setCurrentScreen('Home')` = 목차에서 2장을 선택
- 리렌더링 = 책을 2장으로 넘김

---

## 🚀 다음 단계

이제 React Navigation을 사용하면 더 강력한 네비게이션 기능을 사용할 수 있습니다:
- 히스토리 관리 (뒤로가기)
- 파라미터 전달
- 탭 네비게이션
- 등등...

하지만 현재 구조도 충분히 작동합니다! 🎉
