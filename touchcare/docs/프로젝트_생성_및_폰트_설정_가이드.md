# 프로젝트 생성 및 프리텐다드 폰트 설정 가이드

## 1️⃣ 사전 준비

### Node.js 설치 확인
```bash
node --version
npm --version
```

**설치되어 있지 않다면:**
- [Node.js 공식 사이트](https://nodejs.org/)에서 LTS 버전 다운로드 및 설치
- 설치 후 터미널 재시작

---

## 2️⃣ Expo 프로젝트 생성

### 프로젝트 생성
```bash
npx create-expo-app@latest touchcare-mobile --template blank
```

또는 TypeScript 템플릿 사용:
```bash
npx create-expo-app@latest touchcare-mobile --template blank-typescript
```

### 프로젝트 디렉토리로 이동
```bash
cd touchcare-mobile
```

### 프로젝트 구조 확인
```
touchcare-mobile/
├── App.js (또는 App.tsx)
├── assets/
├── node_modules/
├── package.json
└── app.json
```

---

## 3️⃣ 프리텐다드 폰트 적용

### 방법 1: expo-google-fonts 사용 (권장 - 간단)

#### 1. 패키지 설치
```bash
npx expo install expo-font @expo-google-fonts/pretendard
```

#### 2. App.js 수정
```javascript
import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import { useFonts } from 'expo-font';
import * as SplashScreen from 'expo-splash-screen';
import { useEffect } from 'react';

// 스플래시 스크린을 자동으로 숨기지 않도록 설정
SplashScreen.preventAutoHideAsync();

export default function App() {
  const [fontsLoaded] = useFonts({
    'Pretendard-Regular': require('@expo-google-fonts/pretendard/Pretendard_400Regular.ttf'),
    'Pretendard-Medium': require('@expo-google-fonts/pretendard/Pretendard_500Medium.ttf'),
    'Pretendard-SemiBold': require('@expo-google-fonts/pretendard/Pretendard_600SemiBold.ttf'),
    'Pretendard-Bold': require('@expo-google-fonts/pretendard/Pretendard_700Bold.ttf'),
  });

  useEffect(() => {
    if (fontsLoaded) {
      SplashScreen.hideAsync();
    }
  }, [fontsLoaded]);

  if (!fontsLoaded) {
    return null; // 폰트 로딩 중
  }

  return (
    <View style={styles.container}>
      <Text style={styles.text}>안녕하세요! TouchCare 앱입니다</Text>
      <StatusBar style="auto" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    fontFamily: 'Pretendard-Regular',
    fontSize: 20,
  },
});
```

---

### 방법 2: 직접 폰트 파일 사용 (더 많은 옵션)

#### 1. 프리텐다드 폰트 다운로드
- [Pretendard GitHub](https://github.com/orioncactus/pretendard)에서 폰트 파일 다운로드
- 또는 CDN에서 다운로드: https://cdn.jsdelivr.net/gh/orioncactus/pretendard@1.3.6/dist/web/static/

#### 2. 폰트 파일 배치
```
touchcare-mobile/
├── assets/
│   └── fonts/
│       ├── Pretendard-Regular.otf
│       ├── Pretendard-Medium.otf
│       ├── Pretendard-SemiBold.otf
│       └── Pretendard-Bold.otf
```

#### 3. expo-font 설치
```bash
npx expo install expo-font
```

#### 4. App.js 수정
```javascript
import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import { useFonts } from 'expo-font';
import * as SplashScreen from 'expo-splash-screen';
import { useEffect } from 'react';

SplashScreen.preventAutoHideAsync();

export default function App() {
  const [fontsLoaded] = useFonts({
    'Pretendard-Regular': require('./assets/fonts/Pretendard-Regular.otf'),
    'Pretendard-Medium': require('./assets/fonts/Pretendard-Medium.otf'),
    'Pretendard-SemiBold': require('./assets/fonts/Pretendard-SemiBold.otf'),
    'Pretendard-Bold': require('./assets/fonts/Pretendard-Bold.otf'),
  });

  useEffect(() => {
    if (fontsLoaded) {
      SplashScreen.hideAsync();
    }
  }, [fontsLoaded]);

  if (!fontsLoaded) {
    return null;
  }

  return (
    <View style={styles.container}>
      <Text style={styles.text}>안녕하세요! TouchCare 앱입니다</Text>
      <StatusBar style="auto" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    fontFamily: 'Pretendard-Regular',
    fontSize: 20,
  },
});
```

---

## 4️⃣ 폰트 사용하기

### 기본 사용법
```javascript
const styles = StyleSheet.create({
  regular: {
    fontFamily: 'Pretendard-Regular',
    fontSize: 16,
  },
  medium: {
    fontFamily: 'Pretendard-Medium',
    fontSize: 16,
  },
  semiBold: {
    fontFamily: 'Pretendard-SemiBold',
    fontSize: 16,
  },
  bold: {
    fontFamily: 'Pretendard-Bold',
    fontSize: 16,
  },
});
```

### 전역 스타일 설정 (권장)

#### 1. constants 폴더 생성
```
touchcare-mobile/
└── constants/
    └── Typography.js
```

#### 2. Typography.js 생성
```javascript
export const Typography = {
  // 폰트 패밀리
  fontFamily: {
    regular: 'Pretendard-Regular',
    medium: 'Pretendard-Medium',
    semiBold: 'Pretendard-SemiBold',
    bold: 'Pretendard-Bold',
  },
  
  // 폰트 크기
  fontSize: {
    xs: 12,
    sm: 14,
    base: 16,
    lg: 18,
    xl: 20,
    '2xl': 24,
    '3xl': 30,
    '4xl': 36,
  },
  
  // 미리 정의된 텍스트 스타일
  text: {
    h1: {
      fontFamily: 'Pretendard-Bold',
      fontSize: 36,
    },
    h2: {
      fontFamily: 'Pretendard-Bold',
      fontSize: 30,
    },
    h3: {
      fontFamily: 'Pretendard-SemiBold',
      fontSize: 24,
    },
    body: {
      fontFamily: 'Pretendard-Regular',
      fontSize: 16,
    },
    caption: {
      fontFamily: 'Pretendard-Regular',
      fontSize: 14,
    },
  },
};
```

#### 3. 사용 예시
```javascript
import { Typography } from './constants/Typography';

const styles = StyleSheet.create({
  title: {
    ...Typography.text.h1,
    color: '#303F9F',
  },
  body: {
    ...Typography.text.body,
    color: '#37474F',
  },
});
```

---

## 5️⃣ 개발 서버 실행

### 서버 시작
```bash
npm start
```

또는
```bash
npx expo start
```

### Expo Go 앱으로 실행
1. 스마트폰에 Expo Go 설치
2. QR 코드 스캔
3. 앱 실행 및 폰트 확인

---

## 6️⃣ 문제 해결

### 폰트가 적용되지 않을 때
1. **폰트 파일 경로 확인**: `require()` 경로가 정확한지 확인
2. **폰트 이름 확인**: `fontFamily`에 등록한 이름과 일치하는지 확인
3. **앱 재시작**: 폰트 변경 후 앱을 완전히 종료하고 다시 실행
4. **캐시 클리어**: `npx expo start --clear`

### 폰트 로딩이 느릴 때
- `SplashScreen.preventAutoHideAsync()` 사용하여 폰트 로딩 완료까지 스플래시 화면 유지
- 폰트 파일 크기 최적화 (필요한 weight만 포함)

---

## 7️⃣ 체크리스트

- [ ] Node.js 설치 확인
- [ ] Expo 프로젝트 생성
- [ ] expo-font 패키지 설치
- [ ] 프리텐다드 폰트 파일 준비
- [ ] App.js에 폰트 로딩 코드 추가
- [ ] 폰트 사용 테스트
- [ ] 개발 서버 실행 및 확인

---

## 📚 참고 자료

- [Pretendard GitHub](https://github.com/orioncactus/pretendard)
- [Expo Font 문서](https://docs.expo.dev/guides/using-custom-fonts/)
- [@expo-google-fonts/pretendard](https://www.npmjs.com/package/@expo-google-fonts/pretendard)

---

**준비 완료! 이제 프리텐다드 폰트가 적용된 앱을 개발할 수 있습니다! 🚀**
